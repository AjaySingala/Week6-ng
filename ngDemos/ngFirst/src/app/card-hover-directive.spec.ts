import { CardHoverDirective } from './card-hover-directive';

describe('CardHoverDirective', () => {
  it('should create an instance', () => {
    expect(new CardHoverDirective()).toBeTruthy();
  });
});
